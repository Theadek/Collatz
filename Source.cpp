#include <iostream>
#include "Heap.h"

using namespace std;

int main()
{
	int n, q, k;
	char c;
	Heap *heap = new Heap;
	cin >> n;
	long long int *tab = new long long int[n];
	for (int i = 0; i < n; i++)
	{
		cin >> tab[i];

	}

	cin >> q;
	for (int i = 0; i < q; i++)
	{

	}





}